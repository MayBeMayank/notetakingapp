# Frontend environment variables (Vite — must be prefixed with VITE_)
# Base URL for backend API calls. In dev, the Vite proxy forwards /api to the backend,
# so the default relative path works without setting this.
VITE_API_URL=/api
